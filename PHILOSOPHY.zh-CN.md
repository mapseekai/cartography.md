# 设计哲学

cartography.md 把地图设计系统视为持久的视觉身份，而不是临时生产配方。当当前任务、数据集、工具链和输出发生变化时，一份 `CARTOGRAPHY.md` 仍应继续有用。

English: [PHILOSOPHY.md](PHILOSOPHY.md)

## prose 承载设计

专业制图判断无法被压缩成一张属性清单。文档的 Markdown 应描述一个鲜明、可辨识的视觉世界：什么应该安静或紧迫、什么值得注意、如何控制密度、哪些取舍保护理解，以及哪些做法绝不能侵蚀家族特征。

泛化形容词并不足够。“简洁、现代”几乎不能给 Agent 任何方向；“温暖纸色、浅淡水色、节制墨色与宽松标注间距”则建立了能够指导大量决策的完整世界。

## token 提供准确上下文

token 用于需要精确和复用的位置：色值、字体、尺寸和透明度。它们让 prose 能引用同一套稳定词汇，并防止细小数值在反复应用中漂移。

token 是设计判断的上下文，不是某个生产目标的指令。精确 token 引用必须在同一文档中解析。当工具能够确定准确值与 prose 冲突时，以 token 为准，并应报告该矛盾。

## 身份应适应变化的内容

持久的设计系统描述关系，而不是当前清单。它规定哪些语义角色应安静、提供上下文、成为焦点、表示选中或发出警示，却不记录今天的字段名或对象列表。这样，同一个视觉家族可以面对未知内容，同时不假装未知事实已经得到理解。

Agent 在应用视觉身份时必须保留语义含义。强调可以让选中对象更容易找到，但不能把普通对象变成警告，也不能抹去上下文与焦点之间的区别。

## 层级是组织原则

每个视觉选择都参与层级。背景、上下文、主体、焦点和关键状态应通过协调的对比、粗细、大小、间距与纹理保持可区分。注意力是有限的：当一切都被强调，就没有任何事物真正突出。

因此，构图和叠放应被描述为长期关系，而不是当前对象列表。文档应解释地图变密时什么先退让，以及哪些信息必须保留呼吸空间。

## 比例尺意味着渐进披露

比例尺行为应描述可辨识的阅读阶段，而不编码目标专属阈值。远距离阅读时保留大的空间故事；距离拉近后，以有意控制的节奏显露局部结构。简化、聚合、标注密度与符号细节可以变化，但视觉家族必须连续。

问题不只是哪些内容出现或消失，而是每个阶段是否仍在表达同一种层级和身份。

## 状态必须保留底层含义

hover、selection、alert、invalid 等临时状态属于视觉语言。它们应当彼此可区分，同时不破坏对象的基础角色，也不遮蔽周边上下文。

当含义关键时，状态差异不应只依赖色相。形状、轮廓、图案、粗细、位置和文字可以形成冗余提示，让警示与选中始终保持不同。

## 无障碍是设计约束

无障碍属于长期设计判断，而不是事后补充。显式对比关系可以建立可确定的最低要求；prose 则应覆盖色彩冗余、小尺寸标注可读性、密度和关键状态处理。

通过已声明的对比检查，刻意不等同于完成无障碍评审。真实输出仍需在实际环境中评价。

## 保留核心不能理解的内容

格式允许命名空间扩展和未知内容。解析器与工具应保留自己不解释的信息。已知的疑似拼写错误可以收到 warning，但陌生键不能只因当前核心对此没有意见就被静默丢弃。

这一保留规则让专业社群可以扩展文档，而不必把每个领域问题都强塞进公共 schema。

## 只验证可确定的内部事实

核心验证有意限制在文档范围。它可以检查安全 YAML、front matter schema、章节、引用、已知 token 类型、省略项和已声明对比关系；它无法推断 prose 质量、当前数据真伪、任务适配度或产生产物是否成功。

这条边界让 finding 保持诚实。自动化应在事实可知处严格，并明确指出专业评审与运行时上下文从哪里开始。
