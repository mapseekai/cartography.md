# Cartography.md 理念

Cartography.md 的存在，是因为地图样式不只是渲染器属性的集合。一幅有用的电子地图，是目的、数据、比例尺、感知、交互与实现之间协商形成的关系。

## 样式是执行；契约是意图

MapLibre Style Specification 为源、图层、过滤器、表达式、绘制与布局提供了精确的语言。这种精确性必不可少，但它位于若干通常被隐含处理的决策之后：

- 地图帮助人们做出什么决定；
- 哪些数据具有权威性；
- 哪些区分属于名义型、有序型、定量型、不确定型或关键型；
- 每种比例尺下哪些对象应当占据主导；
- 每种含义由哪些视觉通道承载；
- 交互如何能改变强调程度，而不改变业务事实。

`CARTOGRAPHY.md` 使这些决策得以持久化。生成的 `style.json` 仍是可执行输出。

## 机器可读的值与人类判断应当并存

仅有 token 无法解释为何危险红色应当稀少、为何标注应先于网络线段消失，或为何不确定性字段只能将不透明度降低至安全下限。仅有散文也无法保证 Agent 两次使用相同的颜色或字段名。

因此，该格式由两个同等重要的部分构成：

- YAML 为 Agent 提供精确的值与关系；
- Markdown 为 Agent 提供理由、例外情况及冲突解决指引。

二者无法相互替代。

## 数据含义先于视觉处理

Agent 不得推断名为 `level` 的字段是有序的、较大的数字更危险，或未知类别属于正常。该契约将语义角色绑定到实际字段，而 `DATA_PROFILE.json` 则记录已观测的事实，例如几何类型、值域、单位、可空性、zoom 可用性、密度及稳定标识符。

只有当其数据假设有效时，制图选择才有效。

## 视觉通道需要所有权

地图常常需要同时呈现多个属性。若没有明确的所有权模型，Agent 会反复过载使用颜色，产生美观但不可读的结果。

Cartography.md 将颜色、明度、宽度、尺寸、不透明度、图案、形状和外框视为有限资源。一个通道应有一个主要语义所有者。经过审慎设计的组合必须声明并说明。

关键含义可能需要第二个通道，以便在色觉差异、显示设备不佳、影像背景以及快速操作使用的情况下仍可区分。

## zoom 是设计的一部分，而非事后补充

电子地图不是静态海报。随着 zoom 变化，表达形式可能经历隐藏、聚合、简化、完整几何、标注和编辑细节等状态。该契约会明确描述这些转换。

样式级可见性并不等同于几何制图综合。简化、合并、移位和拓扑保护通常属于上游的数据或瓦片生产环节。该格式如实保留这一边界。

## 选择不得改写事实

交互是强调，而非新的业务分类。被选中的故障管道仍是故障状态。被悬停的维护资产仍处于维护中。附加外框、描边、光晕或受控的宽度变化，通常比替换主体颜色更安全。

同一原则也适用于质量标记、验证错误和权限：独立的含义不应彼此悄然覆盖。

## 美学是受约束的关系

“美丽”并非单一分数。制图质量源自层级、图形与背景的分离、节奏、密度、平衡、克制与任务适配。这些属性会受到真实数据和真实视口的影响。

参考验证器检查确定性的前提条件与已声明的证据。它并不假装语法树能够完整判断一幅渲染出的地图。渲染场景 fixture 与人工评审仍是符合性的组成部分。

## 溯源使 Agent 编辑可维护

生成和修改后的图层应说明产生它们的语义组、编码及 token 引用。溯源支持小型差异、漂移检测、所有权保护和未来的 Agent 修订。

目标并非让每个图层都冗长，而是使重要决策可以恢复。

## 未知内容应当保留

组织将针对行业、安全制度、渲染器和质量门禁扩展该格式。未知的命名空间键和 Markdown 章节应当保留。当工具无法评估某个扩展时，应当发出警告，但不得将其抹除。

## 验证是分层的

本项目将以下内容分开：

1. 文档有效性；
2. 数据契约有效性；
3. 官方 MapLibre 样式有效性；
4. Cartography.md 到样式的一致性；
5. 渲染证据；
6. 任务评审。

通过较早的层级，永远不能证明较晚的层级。地图可以在语法上有效，却仍具有误导性、缺乏无障碍支持或视觉上不连贯。

## 最终原则

持久的地图样式连接四类事物：

- **token（tokens）** 提供精确的值；
- **数据绑定（data bindings）** 提供事实；
- **散文（prose）** 提供判断；
- **验证证据（validation evidence）** 提供信心。

Cartography.md 将这四类事物保留在一个供 Agent 阅读的契约中。
