# 0.4.0 implementation and verification

Implemented against CARTOGRAPHY_0.4.0_PLAN.md on 2026-09-07. Package and format are both 0.4.0. Changes remain local; no tag, npm publication or remote release was performed.

| Plan area | Delivered evidence |
|---|---|
| SPEC / SCALE / STATE / THEMATIC | Specification §§3.1.1, 9.2.1, 9.3, 9.7; explicit visual meanings, stable base expressions, stage invariants, opaque combined states, thematic guidance and runtime boundary. |
| SCHEMA / TYPE / LINT | Versioned JSON Schema and TypeScript contract, unsupported-version diagnostic, existing reference/type checks retained, new positive and negative regressions. |
| AGENT | Both cartography skills updated with decision order, conflict handling, explicit persistent-edit boundary and degradation reporting. |
| INIT | widths/sizes/spacing separated through IR and consolidation; signed offsets literal; roles/states evidence-only; full typography comparison and extension-preserving YAML; semantic token sharing candidates; provenance/statistics/scales in reports; nine TODO-based chapters. |
| EXAMPLE | Quiet Atlas revised; Technical Network and Thematic Evidence added and linted. |
| AUTHOR / DOC / MIGRATION | English/Chinese authoring and migration guides, README/API/philosophy/changelog updates and English visual-semantics companion. Complete normative spec remains Chinese at both established paths; the companion covers the 0.4.0 additions. |
| TEST / RELEASE | Core, initializer, data-profile, schema, package and release checks; adapter acceptance specification; check now builds before tests that inspect distribution artifacts. |

Format validation: passed. `pnpm run check` passed; after the final initializer-only change, its typecheck and complete suite passed again. Counts: core 149, data-profile 134, initializer 59, release metadata 8 — 350 tests total. All three examples lint successfully. `git diff --check` is clean. Release metadata resolves v0.4.0 to npm tag latest with prerelease=false.

Design review: reviewed example component purposes and limits, scale baselines and invariants, critical-selected preservation, context suppression, and missing/unknown/zero/not-applicable distinction. Quiet Atlas explicitly maintains default state only and separates waterway/road scales. Network casing is 4px + 2 × 1px = 6px; selection retains critical color and dash. Thematic legend guidance preserves availability and sequence direction.

Target validation: initializer fixtures exercise MapLibre style JSON, QGIS/QGZ, ArcGIS LYRX/STYLX and SLD extraction through lint-clean drafts. Radius-to-diameter and literal dash-unit conversion are checked; icon aspect ratio, group opacity, target syntax and capability-loss expectations are specified in [adapter contracts](../adapter-contracts.md). These do not claim that production output adapters or pixel-equivalent renderings were built.

Visual review: no renderer output was requested or generated; rendered readability and accessibility were not measured. They remain target-artifact checks, separate from core format validity.

Impact review: GitNexus analyzed this checkout at base 37e5e1f and identified parser/lint/diff paths as high impact. Its index omits initializer code under .agents, so initializer dependencies were inspected directly and covered by integration tests; graph silence was not treated as evidence of no impact.
